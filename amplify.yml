version: 1
frontend:
  phases:
    preBuild:
      commands: []

    build:
      commands: []
  artifacts:
    baseDirectory: /static/dist
    files:
      - '**/*'
  cache:
    paths:
      - static/node_modules/**/*
